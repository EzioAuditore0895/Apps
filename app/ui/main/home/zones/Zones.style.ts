import createStyles from "../../../../base/base.style";

const styles = createStyles({
  loading: {
    marginTop: 10,
    marginBottom: 10,
    alignItems: "center",
  },
  listStyle: {
    padding: 20,
  },
  listContainerStyle: {
    paddingBottom: 80,
  },
});

export default styles;
